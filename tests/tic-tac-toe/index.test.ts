import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import {} from "@ttt/index";

describe("ticTacToe", () => {
  it("outputs a prompt to the CLI on start", () => {});
  it("accepts valid input from players", () => {});
  it("closes the terminal connection when prompted", () => {});
  it("accepts popular 'other ways' to close the terminal (q, end)", () => {});
});
